# CLIConductor 目标定义

> 不含任何技术栈信息。技术选型在此文档完成后单独决策。

---

## 一、项目身份

**一句话**：一个中间层，让主 Agent 调度管理多个第三方 AI CLI 进程，同时人类可随时观察、插话、接管任意进程。

**不是什么**：新的 AI Agent / IDE 插件 / 终端模拟器

**是什么**：管理 CLI 进程生命周期的中间件 + 连接主 Agent、人类、子 CLI 的事件管道

---

## 二、角色与关系

```
人类 (1人, 本地PC)
  │
  ├─ 通过多种渠道与系统交互
  ├─ 观察所有子 CLI 的运行状态
  ├─ 随时介入任意子 CLI
  │
主 Agent (外部AI API / Agent 程序)
  │
  ├─ 读取所有子 CLI 的事件流和结果
  ├─ 向任意子 CLI 派发任务
  ├─ 统筹多 Worker 的进度和结果汇报
  ├─ 拥有独立的持久化记忆空间
  │
子 CLI (CodeBuddy Code 等)
  │
  └─ 执行主 Agent 或人类派发的具体任务
```

---

## 三、核心能力

### C1 — 信息路由

系统的中枢。所有消息——无论来自程序化 API、Web Dashboard、命令行还是未来 QQ/微信——都通过统一的路由层进入系统，分发到正确的 Worker 或主 Agent。

| 入口渠道 | 使用者 | 用途 |
|---------|--------|------|
| 程序化接口 | 主 Agent | 派发任务、读取事件流、管理 Worker |
| Web Dashboard | 人类 | 实时观察、插话、接管 |
| 命令行 | 人类 | 快速操作（启动/查看/停止）|
| 外部消息渠道 | 人类 | QQ/微信/钉钉等（初期预留，不实现）|

路由规则：
- 每条消息都有明确的目标（具体 Worker / 主 Agent / 系统）
- 同时抵达的目标同一 Worker 的消息按 FIFO 排队
- 同一 Worker 同时只能被一个来源写入

### C2 — 本地化记忆

系统的持久化存储。分为三层：

| 层级 | 内容 | 存储要求 |
|------|------|---------|
| Worker 会话 | 每个 Worker 的 CLI 会话 ID + 完整对话历史 | 持久化，重启可恢复，支持回看 |
| 主 Agent 记忆 | 跨 Worker 的全局记忆 | 与 Worker 记忆隔离，互不污染 |
| 系统元数据 | Worker 注册表、配置、有价值会话的归档 | 持久化 |

关键要求：
- Worker 对话历史完整保存，随时可回看
- 有价值的历史会话可标记归档，长期保留
- 主 Agent 记忆与各 Worker 记忆完全隔离

### C3 — 进程调度

Worker CLI 进程的完整生命周期管理。

---

## 四、功能需求

### F1 — Worker 生命周期

| 功能 | 描述 |
|------|------|
| 创建 | 启动一个子 CLI 进程，分配独立工作目录 |
| 列表 | 查看所有 Worker 的 ID、名称、状态、当前任务 |
| 终止 | 强制停止指定 Worker 及其 CLI 进程 |
| 恢复 | 利用本地存储的会话信息恢复之前的上下文 |
| 并行 | 多个 Worker 同时执行不同任务，互不阻塞 |

### F2 — Agent 任务派发

| 功能 | 描述 |
|------|------|
| 派发 | Agent 通过程序化接口向指定 Worker 发送任务（文本描述）|
| 实时流 | Agent 实时接收 Worker 的流式输出和中间事件 |
| 结果 | Agent 获取 Worker 的任务执行结果（含成功/失败/错误信息）|
| 暂停 | Agent 暂停向指定 Worker 派发新任务（如等待人类介入结束）|

### F3 — 人类观察

| 功能 | 描述 |
|------|------|
| 实时输出 | 人类实时看到每个 Worker 的流式输出 |
| 事件分类 | 输出按类型区分：思考过程、文本回复、工具调用、工具结果 |
| 状态概览 | 一眼看到所有 Worker 的运行状态 |
| 会话回看 | 查看任意 Worker 的历史对话记录 |

### F4 — 人类插话

| 功能 | 描述 |
|------|------|
| 注入消息 | 人类向指定 Worker 发送一条文本消息 |
| 上下文持续 | 注入的消息作为该 Worker 会话的一部分，AI 能理解前文 |
| 不打断 | 当前任务正常完成后再处理注入消息 |

### F5 — 人类接管

| 功能 | 描述 |
|------|------|
| 接管终端 | 人类完全控制 Worker 的 CLI 交互界面 |
| 恢复上下文 | 使用本地存储的会话历史，接管时 AI 记得之前的对话 |
| 退出接管 | 退出后，Agent 恢复对该 Worker 的程序化控制 |

### F6 — 多 CLI 适配

| 功能 | 描述 |
|------|------|
| 适配器接口 | CLI 交互抽象为可替换的适配器，统一方法签名 |
| 默认适配器 | 内置 CodeBuddy Code 适配器 |
| 其他 CLI | Claude Code、Copilot CLI 等通过新适配器接入 |

### F7 — 日志记录

| 功能 | 描述 |
|------|------|
| 系统日志 | 记录系统启动、关闭、Worker 创建/终止等关键事件 |
| Worker 日志 | 记录 Worker 接收的任务、执行状态、异常信息 |
| 持久化 | 日志写入本地文件，重启不丢失 |
| 控制台输出 | 同时输出到控制台，方便开发调试 |

### F8 — 崩溃恢复

| 功能 | 描述 |
|------|------|
| Worker 隔离 | 单个 Worker 崩溃不影响其他 Worker 和主系统 |
| 自动恢复 | Worker 异常退出后，系统自动尝试重启并恢复会话上下文 |
| 优雅关闭 | 系统退出时确保所有子进程被终止 |
| 数据保护 | 崩溃时不损坏已持久化的会话数据 |

### F9 — 全局记忆

| 功能 | 描述 |
|------|------|
| 记忆空间 | 主 Agent 拥有独立的持久化存储 |
| 跨 Worker | 主 Agent 可以将任意 Worker 的信息写入自己的记忆 |
| 隔离 | 主 Agent 记忆与 Worker 记忆互不污染 |
| 持久 | 系统重启后记忆不丢失 |

---

## 五、不做的事

| 不做 | 理由 |
|------|------|
| 实现主 Agent 的"大脑"逻辑 | 主 Agent 是外部的，本项目只做管理中间件 |
| 自动任务拆分 | 主 Agent 自己决定谁做什么 |
| 任务优先级 | 先来后到即可 |
| 多用户 | 单人类用户 |
| 认证/权限 | 本地单用户不需要 |

---

## 六、非功能需求

### 运行环境
- 操作系统：Windows（本地开发运行）
- 网络：本地回环，不需要外部访问
- 用户：单人类用户

### 性能
- 2-5 个 Worker 并行，无明显延迟
- 事件从 CLI 输出到 Dashboard 显示的延迟 < 500ms

### 可靠性
- Worker 崩溃不影响其他 Worker（详见 F8）
- 会话数据持久化到磁盘，重启可恢复
- 本地存储文件损坏时不影响系统启动（降级运行）

### 开发
- 从最小完整链路起步
- 每步有一条可验证的端到端链路
- 接口预留扩展点，不提前写扩展代码
- 不确定的技术点先做微型实验

---

## 七、成功标准

| # | 标准 |
|---|------|
| 1 | 主 Agent 向 Worker-1 和 Worker-2 各派一个任务，两个同时执行 |
| 2 | 人类在 Web 页面实时看到两个 Worker 的流式输出和分类事件 |
| 3 | 人类在 Web 页面向 Worker-1 注入一条消息，Worker-1 在后续对话中响应 |
| 4 | 人类接管 Worker-2 的终端，直接交互后退出，Agent 恢复控制 |
| 5 | 系统重启后，Worker 的会话 ID 和对话历史可从本地存储恢复 |
| 6 | 主 Agent 在不同的 Worker 任务间能访问自己的全局记忆 |
| 7 | 系统支持至少两种不同 CLI 作为 Worker |
| 8 | 人类通过外部消息渠道（QQ/微信等）与系统交互 |
| 9 | 系统无人值守运行超过 24 小时 |
